sceneParameters:
{
    ambiantOcclusion: {
        samples = 0;
        radius = 0.0;
    };
    ambientLight: {
        color: { r = 0; g = 0; b = 0; };
        intensity = 0.0;
    };
    ambientDiffuse: {
        color: { r = 0; g = 0; b = 0; };
        intensity = 0.0;
    };
    samplesPerPixel = 100;
    adaptiveSampling = false;
};

camera:
(
    {
        resolution: { width = 800; height = 600; };
        rotation: { x = 0.0; y = -0.042612; z = -1.0; };
        position: { x = 50.0; y = 52.0; z = 295.6; };
        fieldOfView = 90.0;
    }
);

objects_clusters:
(
    "shared/walls_room.cfg",
    "shared/octane_mesh.cfg"
);

lights:
{
    directional_light = (
        {
            position: { x = 0.0; y = 0.0; z = 0.0; };
            direction: { x = -0.45; y = -1.0; z = -0.25; };
            color: { r = 255; g = 240; b = 220; };
            intensity = 8.2;
        }
    );
    point_light = (
        {
            position: { x = 68.0; y = 82.0; z = 138.0; };
            color: { r = 255; g = 230; b = 200; };
            intensity = 2000.0;
            radius = 2.0;
        }
    );
};